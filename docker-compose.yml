version: '3.8'

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DEBUG=True
      - DJANGO_SECRET_KEY=your-secret-key
      - DATABASE_URL=mysql://root:dvCvnhiYxcpPfUEhmXuEupPiJvXtbeNo@roundhouse.proxy.rlwy.net:54048/railway  # เชื่อมต่อกับ Railway
